<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Container for the back button, spinner, and search bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <!-- The Spinner for different intolerances -->

        <Button
            android:id="@+id/mealPlanButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="start"
            android:text="@string/mealPlan"/>

        <Spinner
            android:id="@+id/spinner_dietary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Search bar -->
        <EditText
            android:id="@+id/editText_search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/search_here"
            android:layout_marginTop="8dp"
            android:inputType="text"/>
    </LinearLayout>

    <!-- Allows scrolling through the screen viewing the different meal images -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="8dp"/>
</RelativeLayout>
